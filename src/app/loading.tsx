import Loader from "@/components/shared/Loader";

export default function Loading() {
  return (
    <main className="w-screen h-screen flex justify-center items-center px-10">
      <Loader />
    </main>
  );
}
